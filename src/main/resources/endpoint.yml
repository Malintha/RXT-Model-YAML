endpoint:

  concrete: true
  label: Endpoint

  content:
    environment:
      label: Environment
      type: select
      values:
           - None
           - Unknown
           - Dev
           - Testing
           - Production

    url:
      label: URL
      type: text
      regex: http\.\S+\.com
      validationMessage: Please enter a valid URL
